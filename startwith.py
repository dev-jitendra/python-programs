text = "Hello, world!"
prefix = "Hello"
if text.startswith(prefix):
    print("Starts with prefix")
else:
    print("Does not start with prefix")
# The 'startswith()' method checks if 'text' starts with the specified 'prefix'.
